spring:
  application:
    name: xxnjdg-auth-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.100.26:8848 # 配置中心
        file-extension: yaml # 这里指定的文件格式需要和nacos上新建的配置文件后缀相同，否则读不到
